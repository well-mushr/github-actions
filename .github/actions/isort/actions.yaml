---
name: isort
description: 'Runs isort'
inputs:
  enabled:  # id of input
    description: 'Bool to skip the job'
    required: true
    default: 'true'
runs:
  using: "composite"
  if: ${{ inputs.enabled == "true" }}
  steps:
    - name: isort commands
      run: echo "isort commands"
